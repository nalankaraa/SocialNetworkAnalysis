<Window x:Class="SocialNetworkAnalysis.UI.Views.MatrixWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:SocialNetworkAnalysis.UI.Converters"
        mc:Ignorable="d"
        Title="Adjacency Matrix" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#0D0E19">

    <Window.Resources>
        <conv:WeightToColorConverter x:Key="WeightToColorConv"/>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#1F1F2E"/>
            <Setter Property="Foreground" Value="#00E5FF"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#2B2B3D"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#0D0E19"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#151624"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1F1F2E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Cell Style will be applied dynamically in code behind to bind to specific columns, 
             but we define a base here if needed or just styling for the TextBlock inside -->
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Area -->
        <Border Grid.Row="0" Background="#1F1F2E" Padding="20" BorderBrush="#2B2B3D" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Network Adjacency Matrix" Foreground="White" FontSize="22" FontWeight="Bold"/>
                    <TextBlock Text="View connection weights between nodes. Heatmap intensity indicates weight strength." Foreground="#8F90A6" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Simple Legend -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Weak" Foreground="#8F90A6" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <Border Width="100" Height="10" Margin="5,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#1F1F2E" Offset="0"/>
                                <GradientStop Color="#00E5FF" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>
                    <TextBlock Text="Strong" Foreground="#00E5FF" VerticalAlignment="Center" Margin="5,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- The Matrix -->
        <DataGrid x:Name="MatrixGrid" Grid.Row="1" 
                  AutoGenerateColumns="True" 
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  CanUserSortColumns="False"
                  CanUserReorderColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  Background="Transparent"
                  BorderThickness="0"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  RowHeaderWidth="0"
                  ColumnHeaderHeight="40"
                  RowHeight="35"
                  FontSize="13">
        </DataGrid>
    </Grid>
</Window>
