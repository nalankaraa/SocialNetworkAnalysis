<UserControl x:Class="SocialNetworkAnalysis.UI.Views.NodePropertiesPanel"

             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="250">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#8F90A6"/>
            <Setter Property="Margin" Value="0,5,0,2"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#151624"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2B2B3D"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#1F1F2E" BorderBrush="#444466" BorderThickness="1" CornerRadius="8" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="NODE EDITOR" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>

            <!-- Properties Content (No Tabs) -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                <StackPanel>
                    <!-- ID (Readonly) -->
                    <TextBlock Text="ID"/>
                    <TextBox Text="{Binding Id, Mode=OneWay}" IsReadOnly="True" Opacity="0.5"/>

                    <TextBlock Text="Name"/>
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Club"/>
                    <TextBox Text="{Binding Club, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Role"/>
                    <TextBox Text="{Binding Role, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Activity Level"/>
                    <TextBox Text="{Binding Activity, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Interaction Rate"/>
                    <TextBox Text="{Binding Interaction, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Connection Count"/>
                    <TextBox Text="{Binding ConnectionCount, Mode=OneWay}" IsReadOnly="True" Opacity="0.5"/>

                </StackPanel>
            </ScrollViewer>

            <!-- Footer Actions -->
            <StackPanel Grid.Row="2" Margin="0,10,0,0">
                <Button Content="💾 Update &amp; Save" Command="{Binding DataContext.UpdateNodeCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                    CommandParameter="{Binding}"
                                    Background="#00E5FF" Foreground="Black" FontWeight="Bold" Margin="0,0,0,5" Padding="5,10">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Content="🗑 Delete Node" Command="{Binding DataContext.RemoveNodeCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                        CommandParameter="{Binding}"
                        Background="#FF3333" Foreground="White" Margin="0,0,0,5" Padding="5,10">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
